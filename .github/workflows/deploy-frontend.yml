name: Deploy Frontend → GitHub Pages

on:
  push:
    branches: [main]
    paths:
      - 'apps/web/**'
      - '.github/workflows/deploy-frontend.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Build static export
        env:
          NEXT_PUBLIC_API_URL: ${{ secrets.RENDER_API_URL }}
          NEXT_PUBLIC_WS_URL:  ${{ secrets.RENDER_WS_URL }}
          NEXT_PUBLIC_GATEWAY_URL: ${{ secrets.RENDER_GATEWAY_URL }}
        run: npm run build -- --filter=@aegis/web

      - name: Add .nojekyll
        run: touch apps/web/out/.nojekyll

      - name: Flatten structure
        run: |
          cd apps/web/out
          if [ -d "AEGIS" ]; then
            cp -r AEGIS/* .
            rm -rf AEGIS
          fi

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: apps/web/out

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
